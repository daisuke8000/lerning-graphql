{
  "name": "server",
  "version": "1.0.0",
  "description": "graphql-server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon index"
  },
  "license": "MIT",
  "devDependencies": {
    "apollo-server": "^3.10.2",
    "graphql": "^16.6.0"
  }
}
